<div class="col s12">
  <ul class="collapsible" data-collapsible="accordion">
    <li>
      <div class="collapsible-header profile-header"><span class="badge">자세히 보기</span><i class="fa <%= sex_icon(find_by_key(@student_data, 'gen')) %>" aria-hidden="true"></i><%= find_by_key(@student_data, 'kornm') %> 학우님</div>
      <div class="card white darken-1 collapsible-body profile-body">
        <div class="card-content dark-text">
          <span class="card-title"><%= find_by_key(@student_data, 'sustnm') %> <%= find_by_key(@student_data, 'mjnm') %> <%= find_by_key(@student_data, 'probshyr') %>학년 (<%= find_by_key(@student_data, 'stdno').split(//).first(4).join %>년도 입학)</span>

        </div>
        <div class="card-action">
          <% unless params[:controller]=='parsers' %>
              <a href="#">이 학우님이 현재 수강하는 과목 목록 보기</a>
          <% end %>
          <a href="#">학우님에게 메세지 남기기</a>
        </div>
      </div>
    </li>
  </ul>
</div>

